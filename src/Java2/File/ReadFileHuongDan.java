package Java2.File;

public class ReadFileHuongDan {
    /* Đọc dữ liệu từ file  FileInputStream
    1. FileInputStream fileInputStream = null;
    2. fileInputStream = new FileInputStream("test.txt"); try cash
    3. finally -> fileInputStream.close();  try cash
    4. StringBuilder stringBuilder = new StringBuilder();
    5. int code; -> while ((code = fileInputStream.read()) != -1)
    6. stringBuilder.append((char) code); -> String conten = stringBuilder.toString(); -> System.out.println(conten);
    7.  char c = (char) code; ->System.out.print(c); nếu ko dùng stringBuilder
     */
}
