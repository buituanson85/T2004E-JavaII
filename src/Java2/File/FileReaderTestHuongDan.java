package Java2.File;

public class FileReaderTestHuongDan {
    /*   đọc dữ liệu từ file theo dòng FileReader
    1. FileReader fileReader = null;
    2. fileReader = new FileReader("test.txt"); try cash
    3. finally  fileInputStream.close(); try cash (ko có if != null)
    4. BufferedReader bufferedReader = null;  khai báo dưới 1. bộ thư viện chạy cùng
    5. finally if (bufferedReader != null) -> bufferedReader.close(); try cash
    6. bufferedReader = new BufferedReader(fileReader);  khai báo dưới 2.
    7.String line; -> while ((line = bufferedReader.readLine()) != null) -> System.out.println(line);
     */
}
